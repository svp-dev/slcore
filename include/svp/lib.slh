m4_ifndef([[SVP_LIB_SLH]],[[m4_define([[SVP_LIB_SLH]],1)

#include "svp_os.h"

m4_define([[svp_abort]], [[__abort()]])

#ifdef __GNUC__
#define likely(X) __builtin_expect(!!(X), 1)
#define unlikely(X) __builtin_expect(!!(X), 0)
#else
#define likely(X) /*likely*/(X)
#define unlikely(X) /*unlikely*/(X)
#endif

]])



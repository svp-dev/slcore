#ifndef SVP_LIB_SLH
#define SVP_LIB_SLH

#include "svp_os.h"

#define svp_abort __svp_abort

#ifdef __GNUC__
#define likely(X) __builtin_expect(!!(X), 1)
#define unlikely(X) __builtin_expect(!(X), 1)
#else
#define likely(X) /*likely*/(X)
#define unlikely(X) /*unlikely*/(X)
#endif

#endif


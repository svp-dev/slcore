#! /bin/sh
# $Id$
if ! test -x $MGSIM/MGAlpha; then
  echo "Can't find simulator? Please set env. var. MGSIM to source directory." >&2
  exit 1
fi
exec $MGSIM/MGAlpha $SIMARGS -t -c $MGSIM/programs/config.ini "$@"
